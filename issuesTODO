ISSUES:

1. Submodules. This is actually a huge issue. Changes within package folders are not getting pushed. 
2. compiling new C++ code into min

TODO
0 figure out how to build projects and objects using Max (in the extras folder). Konrad will like that. 
0 figure out a cleaner way to do changes, like calling doc_update somewhere.
0 figure out submodules
0 experiment with flext
0 see if I can compile new C++ code into min
